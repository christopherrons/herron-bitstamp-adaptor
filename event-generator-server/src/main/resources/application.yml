server:
  port: 8081

spring:
  kafka:
    bootstrap-servers: "localhost:9092"

kafka:
  topic:
    order-data:
      nr-of-partitions: 2

subscription-config:
  uri: wss://ws.bitstamp.net
  channels:
    - live_orders_btcusd